# ─────────────────────────────────────────────────────────────────────────────
#  이 저장소는 **public** 이다. 여기 올라가는 것은 곧 영구 공개다.
#  아래 세 디렉토리는 어떤 경우에도 main 브랜치에 들어가면 안 된다.
# ─────────────────────────────────────────────────────────────────────────────

# 실데이터. 평문 팩트가 여기 놓인다(로컬 확인용). 배포되는 데이터는 data 브랜치의
# 암호문(facts.json.enc)뿐이고, 이 디렉토리는 절대 커밋하지 않는다.
data/

# 검증용 고정본. /api/dashboard 응답이 그대로 들어 있어 **by_user(계정 식별자)와
# by_scenario(태스크 원문)** 까지 포함한다 — 바깥에 나가면 안 되는 것 전부다.
fixtures/

# 개발·검증 도구. loopview 와 같은 규약(문서·ops 는 Mac 에만 둔다).
ops/
*.md

.DS_Store
